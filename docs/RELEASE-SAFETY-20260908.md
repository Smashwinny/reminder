# 2.3.4 用户数据安全审查与交付记录

本轮从用户登录、记录、同步、断网重试和备份恢复路径审查。不能以健康接口或“同步成功”提示证明不会丢数据。

## 已修复的具体故障

- 同步请求发出后新增或删除任务：旧实现用响应整表替换当前列表，即使响应包含所有已上传 ID，也会丢掉请求发出后的修改。现在主线程序列化请求，响应按 ID 和更新时间合并进当时的最新本地集合。
- 账号切换快照失败：旧实现返回后调用方仍写入新令牌。现在返回明确成功状态，目标数据解析失败或快照失败则停止登录；新分区、身份和凭据在同一次提交中保存。
- 同名账号身份改变：同名不能证明是同一人。用户 ID 改变时先显示确认说明，取消保留旧账号；同服务器同名的迁移只有用户确认后才进行。
- 并发登录：迟到的旧登录结果不能覆盖最近一次登录操作。
- 本地数据损坏：不再将解析失败后的空列表上传或保存。保存失败不显示同步成功。
- 账号切换快照被日常同步挤出：快照按用户与用途分别轮转，账号迁移快照不再被普通同步覆盖。
- 网页断网后刷新：任务按账号写入浏览器存储，串行发送同步请求，响应合并保留请求途中新增内容；拒绝身份不匹配或遗漏已上传 ID 的响应。
- 网页无限等待或误导提示：请求有 20 秒超时，出错显示失败；接收结果显示云端与本地任务数量，不把单纯接收当成全部上传完成。
- 服务器文件损坏：损坏的 JSON/非数组数据不再当空库，也不允许覆盖该文件。账号库缺失但仍有历史用户/会话时，拒绝注册替代账号。
- 服务器误覆盖：写入前保留历史文件（每个文件最多 64 份），写入临时文件并 fsync 后改名。历史文件权限为 600，已加入备份脚本范围。
- 服务重启清空日志：服务与隧道日志均改为追加。Docker 构建入口同步包含新增存储模块。
- 版本默认值落后：默认正式构建版本更新为 2.3.4 / 21。

## 验证证据

### 最终生产拓扑与迁移（20:40 后；优先于下方早期观察）

- 实测本机与阿里云使用相同命名隧道令牌、不同持久化目录；此前单次请求一致不足以排除分流。
- 已停止并禁用本机 `reminder-autostart.service`，核对旧命名隧道进程退出；`deploy/CLOUD_PRODUCTION` 与启动脚本防止误启。本机 `reminder internet` 只打开正式网站，daemon/restore 被拒绝。
- 唯一正式实例为阿里云 `/opt/reminder`、`reminder_app`、`reminder_data`；云端隧道与其他项目容器未停止。
- 停写后的本机 73 条与原云端 70 条合并为 73 条、已完成 2 条。原账号作为 `aliasOf` 身份兼容记录，8 个会话记录保留，登录选择当前账号；旧密码重置链接不迁移，当前重置链接保留。
- 部署包含安全存储、网页同步修复与身份别名解析。18 项 Node 测试通过，包括旧会话上传、新旧会话读同库与密码重置撤销。
- 公网连续 8 次现有手机会话下载成功，均 73 条，ID SHA-256 为 `9de4fd3feb9a495d17541fdfc0e7d921f27a61f5873aaf11bfbacf341f830b40`；新网页脚本可见。
- 云端停写前备份 `/opt/reminder/backups/reminder-data-20260908T123952Z.tar.gz`；合并后备份 `reminder-data-20260908T124240Z.tar.gz` 已下载本机并独立解压，任务逐字段等于迁移结果。每天约 03:20 的 `reminder-backup.timer` 已核实运行。
- 手机解锁后打开拾遗显示 73 条、已完成 2 条；最终 UI 明确显示“上次同步 2026/9/8 下午8:44”，无重新登录提示（证据 `tmp/reminder-release-audit/phone-after-sync.xml`）。

### 早期检查记录（当时尚未识别双入口，不能作为最终拓扑依据）

- `node --test sync-server/server.test.js sync-server/safe-storage.test.js sync-server/web-sync.test.js`：16 项通过。
- `:app:testReleaseUnitTest`：2 项实际 JVM 测试通过，覆盖慢响应期间新增、删除、摘要合并与重复响应。
- 网页测试执行真实服务返回的页面脚本，注入慢响应、网络失败、遗漏 ID 和不同账号响应；浏览器缓存保持完整。
- 存储测试从历史文件恢复此前任务数组并逐字段比较，检查权限；损坏账号库无法生成新的同名用户 ID。
- 生产服务已重启加载修复；公网 `/api/healthz` 返回正常。
- 使用已有账号会话读取生产公网 `/api/sync` 与本机接口，两者均为 72 条、已完成 2 条，ID 集合 SHA-256 均为 `5ac8cc3f3b5e3848b3ff6395bd4bbd578894c45b613c9c002fdd1c90d8f2c0ed`。
- 公网首页已包含按账号持久化及串行请求实现。
- 恢复后生产备份已解压到独立目录，账号数为 1，72 个任务 ID 与生产集合一致，历史文件也包含在归档中。
- 正式签名 APK 2.3.4 已覆盖安装到用户手机；未推送 OTA。

## 验证边界与后续上线要求

- 手机正式签名 2.3.4 已安装；本轮未推送 OTA。
- 生产用户的账号库没有进行破坏性故障注入；这类测试在隔离目录中完成。正常真实数据路径已验证。
- 云端已有每日备份，本轮也下载备份到本机并验证；自动异机备份和主动告警仍未配置，扩大用户范围前必须补齐。不能承诺零故障或零数据损失。
- 网页缓存依赖浏览器站点存储；清除站点数据会清除尚未上传的缓存。切换设备前需确认云端已收到任务。
- 手机附件当前仍是本地 URI，只同步任务信息；不能把手机图片、视频在电脑打开当成已支持的功能。
- 已打开的旧网页需要刷新才能获得本次修复。其他用户仍在旧客户端时仍需升级；本轮没有自动向其他设备推送。

本轮保留了工作区已有的账号备份增强；备份脚本新增历史目录覆盖及日志追加。后续发现双入口，因而更正 AGENTS.md、CLAUDE.md 的生产位置，并为旧复盘增加更正说明，保留原文作诊断记录。

## 发布阻断清单

### 追加对抗式审查

- 修复任务字段缺乏验证：拒绝重复 ID、缺少创建时间、错误状态/删除标记、非数字时间、对象型摘要等，避免某端上传后其他端无法解析。
- 区分存储故障与身份失效：账号/任务文件无法读写时返回 503，不能返回会使客户端清除登录的 401。
- 摘要后台捕获单个任务异常，避免未处理 Promise 拒绝终止整个服务；迁移别名按唯一存储身份排队，避免同任务重复摘要。
- 19 项 Node 测试通过。新用例包含 11 种畸形请求、任务库损坏、账号库损坏，并校验任务文件未被更改；已有 73 条任务的时间/计数字段兼容性检查通过。
- 摘要额度已改为服务端 `summary-usage.json` 独立计数，请求前持久化预扣，重试也收费计数；每用户/全站上限分别默认 20/200 次每天，网页任务上限为 3 倍，队列上限 100。UTC 跨日重置，删除任务、修改摘要状态、重启不能绕过。计数故障停止 AI 消费但不阻断任务同步。新增持久化测试后共 20 项通过。
- 额度模块已部署到真实阿里云容器；一次生产 `job` 预留持久化验证成功（计入用户当天 1 次网页任务额度，未调用 API、未新增用户任务）。部署前备份 `reminder-data-20260908T125750Z.tar.gz`。计数文件首次预留才创建。
- 已安装工作站每小时异机备份检查与桌面告警，首次下载云端 `reminder-data-20260908T125750Z.tar.gz`、SHA-256 校验和解压数据关联检查通过：1 个账号、2 个兼容身份、73 条任务。
- 真实 systemd 检查服务 21:07 执行成功（退出 0），桌面测试通知命令成功；21 项回归通过。复制异机备份到隔离目录后启动实际服务，旧手机会话读取 73 条、新账号初始为空且写入不影响旧账号、无效令牌返回 401，均已验证（`tmp/reminder-release-audit/restore-runtime.js`）。
- 审查尚未完成：电脑关机仍可接收的全天候告警渠道待用户确认；还需继续用户流程与异常场景验收。不能将本记录当作完整小范围上线批准。
- 链接抓取追加审查：旧代码先 DNS 检查再独立 fetch，存在二次解析地址变化风险；现在 socket lookup 直接返回已检查地址，所有重定向重新走同样检查。拦截 IPv4/IPv6 内网、映射/过渡地址、云元数据地址及非 80/443 端口；网页与 X 内容接口均走受限抓取，10 秒（X 8 秒）超时、1.5 MB 响应上限。23 项回归通过，包括混合公网/内网 DNS 答案与映射 IPv6 地址。限制针对用户链接，不改变管理员配置的 Kimi API 端点。
- 抓取保护已部署：云端容器读取 `https://example.com` 返回 200，映射 IPv6 回环地址在连接前拒绝；公网旧会话同步成功，任务集合仍 73 条。补充实际 transport 配置和超大响应中止测试后，共 24 项通过。部署前备份 `reminder-data-20260908T131137Z.tar.gz`。

1. 确认公网连接器只指向同一数据源，禁止在本机启动生产隧道。
2. 先完整备份，独立解压验证账号、会话、任务；禁止只备份 tasks.json。
3. 自动测试通过后，在真实公网完成旧会话兼容、双端读写和任务集合核对。
4. 迁移期保留旧身份兼容与回滚备份；失败时停止发布，不重新注册账号“修复”。
5. 正式扩大用户范围前补齐自动异机备份和告警。此清单是发布要求，尚不是自动 CI/CD 部署锁。
