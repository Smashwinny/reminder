# 渐明

一款以颜色深浅呈现任务状态的轻量 Android 提醒应用。

## 第一版功能

- 粘贴或输入内容，立即记录任务
- 新任务颜色最深；查看、进行中、完成分别呈现不同深浅
- 按颜色、创建时间、提醒时间排序
- 设置系统通知提醒
- 完成、恢复与删除任务
- 使用本地 SharedPreferences 持久保存，重启应用不会丢失
- 通过电脑端公网 HTTPS 入口跨网络双向同步

## 电脑端同步

```bash
reminder internet
```

该命令会启动服务、生成高强度同步密钥，并显示一个 HTTPS 公网同步地址。手机和电脑无需连接同一个 Wi-Fi；手机使用蜂窝网络或任意网络均可选择双向同步、仅上传或仅接收。详细说明见 `sync-server/README.md`。

之后找不到同步地址或密钥时，终端输入 `reminder code` 即可随时重新显示。

当任务内容只有一个 `http://` 或 `https://` 链接时，电脑同步端会自动读取公开网页，并使用 Kimi K3 生成中文标题和摘要。处理完成后在手机点击“仅接收”即可看到摘要。

点击任务会先打开完整详情卡片；阅读后颜色以动画渐变更新，当前列表位置保持不变。按颜色产生的新顺序会在下次打开页面时应用，避免正在阅读的任务突然跳走。

## 构建

```bash
./gradlew assembleDebug
```

APK 输出：`app/build/outputs/apk/debug/app-debug.apk`
